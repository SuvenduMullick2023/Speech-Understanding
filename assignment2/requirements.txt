

#!pip install -q -U pip setuptools wheel
#!apt-get install -y -q espeak-ng libsndfile1
torch
torchaudio
transformers
deepfilternet          # Task 1.3 – DeepFilterNet denoiser
openai-whisper         # fallback if HF Whisper has issues
librosa 
numpy 
scipy 
soundfile
nltk 
g2p-en
epitran                # Task 2.1 – proper Hindi G2P -> IPA
dtw-python             # Task 3.2 – DTW prosody warping
jiwer                  # WER evaluation
coqui-tts              # TTS (YourTTS / VITS)
matplotlib 
tqdm
speechbrain
torchcodec
ffmpeg
flite

# ============================================================
# CELL 3: Install indic-nlp-library and clone resources
# ============================================================
indic-nlp-library
#git clone https://github.com/anoopkunchukuttan/indic_nlp_resources.git

#from indicnlp import common
#common.set_resources_path('/content/indic_nlp_resources')

#import nltk
#nltk.download('punkt', quiet=True)
#nltk.download('averaged_perceptron_tagger', quiet=True)